.. _detection:

Structure of the package
========================

``platos_closet`` is build on a main class called ``Planet``, which involves several functions. These functions
are ``noise_level()``, ``is_detectable()`` and ``detectability_check()``. Each one of these functions is 
described as follows

.. automodule:: detection
   :members: